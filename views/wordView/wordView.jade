extends ../layout

block content
    div(class="container")
        h1 #{title}
        each item in results
            div(class="col-lg-8 row")
                h3 #{item.length} Letter Words
                div(class='wordblock ')
                    each word in item.words
                        div(class='word col-lg-2') #{word}
